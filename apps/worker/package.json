{
  "name": "@fishing/worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --env development --local --persist-to ../../.wrangler/state",
    "build": "wrangler deploy --dry-run --env production",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@fishing/shared": "*",
    "hono": "^4.9.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250821.0"
  }
}
